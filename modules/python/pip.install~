#!/usr/bin/env zsh
set -eux

url=https://raw.github.com/pypa/pip/master/contrib/get-pip.py

apt i python wget

tmp_dir="$(mktemp -d)"
get_pip="$tmp_dir/get-pip.py"

wget -O "$get_pip" "$url"

python2 "$get_pip"

rm -rf "$tmp_dir"









